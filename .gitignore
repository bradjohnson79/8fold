node_modules
.turbo
.next
dist
.expo
.DS_Store
.env
.env.*
*.tsbuildinfo

# Artifact/blob sweep (do not commit)
*.log
logs/
coverage/
test-results/
playwright-report/
*.zip
*.tgz
*.bak
*.bak*

# Local backups + runtime uploads
backup/
apps/api/public/uploads/

# Optional local-only marketing media (enable via env when shipped)
apps/web/public/video/*.mp4
apps/web/public/video/*.webm
# Hero video is shipped to production
!apps/web/public/video/hero_video.mp4

# Local AI/ops reports (do not commit)
*_AGENT_REPORT.md
*_REPORT.md
ROUTER_DASHBOARD_*.md
GIT_PREFLIGHT_REPORT.md

# Local-only drizzle-kit config (apps/api uses repo-root config)
apps/api/drizzle.config.*

