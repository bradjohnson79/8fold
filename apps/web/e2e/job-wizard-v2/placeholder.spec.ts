import { test, expect } from "@playwright/test";

test.describe("Job Wizard V2", () => {
  test("placeholder - wizard v2 e2e tests to be added", async ({ page }) => {
    expect(true).toBe(true);
  });
});
