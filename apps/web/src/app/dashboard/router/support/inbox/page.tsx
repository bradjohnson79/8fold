"use client";

import { SupportInbox } from "@/components/support/SupportInbox";

export default function RouterSupportInboxPage() {
  return (
    <SupportInbox
      basePath="/dashboard/router/support"
      newTicketPath="/dashboard/router/support"
    />
  );
}
