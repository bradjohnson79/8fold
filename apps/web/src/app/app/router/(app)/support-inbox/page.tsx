// Phase 2+ rebuild: remove role-specific "senior router inbox" API paths.
// Keep the route for back-compat, but render the canonical support history view.
export { default } from "../support/history/page";

